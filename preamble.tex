\usepackage{amsmath}
% \usepackage{tocloft} % for custom number spacing
\usepackage{booktabs}
\usepackage{graphicx}

\usepackage{multicol}

\setkeys{Gin}{width=\textwidth}
% \usepackage{kpfonts}

\usepackage{fontspec}
% \defaultfontfeatures{Renderer=HarfBuzz}

\usepackage{setspace}

\usepackage[english]{babel}
% \addto\captionsenglish{% \renewcommand{\chaptername}{Lecture}% }
\usepackage{orcidlink}

\graphicspath{{fig/}{images/}}

% \setlength{\cftsecnumwidth}{6.5ex}

\usepackage{pdfpages}
% get texcount to ignore long tables TC:group longtable 1 0 

% numbering for sections
\usepackage{titlesec}

% Define the global numbering style: A, 1, i
\renewcommand{\thesection}{\Alph{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\roman{subsubsection}}

\usepackage{varwidth}

\usepackage[most]{tcolorbox}
% Plain boxed answer in the base version; full styling lives in preamble-local.tex
\newenvironment{answer}{\begin{tcolorbox}}{\end{tcolorbox}}
% --- Page style: header & footer --------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % clear defaults

\makeatletter
% Header marks
\renewcommand{\sectionmark}[1]{\markboth{\thesection\enspace #1}{}}
\renewcommand{\subsectionmark}[1]{\markright{\thesubsection\enspace #1}}

% Native TOC spacing adjustments for Q1(a)(i) numbering
% \@dottedtocline{level}{indent}{number_width}
\renewcommand*\l@section{\@dottedtocline{1}{0em}{3.5em}}
\renewcommand*\l@subsection{\@dottedtocline{2}{1.5em}{4.0em}}
\renewcommand*\l@subsubsection{\@dottedtocline{3}{3.8em}{5.0em}}

\fancyhead[R]{
  \nouppercase{
    \leftmark
    \ifx\rightmark\@empty
    \fi
  }
}
\makeatother

% Footer: centered page number
\fancyfoot[C]{\thepage}
\setlength{\headheight}{15pt}

% listing & packages for code in appendix 
% \usepackage{listings}
% \usepackage{inconsolata}
% \lstset{basicstyle=\ttfamily\footnotesize,breaklines=true,columns=fullflexible,upquote=true,numbers=left,numberstyle=\tiny,frame=single}

\definecolor{myttgreen}{HTML}{188038}
\definecolor{myttred}{HTML}{C62828}
\newcommand{\greentt}[1]{\texttt{\color{myttgreen}#1}}
\newcommand{\redtt}[1]{\texttt{\color{myttred}#1}}

% check for custom local file
\IfFileExists{preamble-local.tex}
  {\input{preamble-local.tex}}
  {% public dummy fallback
   \newcommand{\hdrleft}{COURSE 20XX/XX -- Group X}%
   \newcommand{\coverfile}{}}

% Left header: subtitle from YAML
\fancyhead[L]{\hdrleft}

% --- First page: cover sheet --------------------------------
\makeatletter
\AtBeginDocument{%
  \ifx\coverfile\@empty\else
    \IfFileExists{\coverfile.pdf}%
      {\includepdf[pages=1]{\coverfile}\clearpage}{}%
  \fi}
\makeatother